import { View, Text } from 'react-native';
import React from 'react';

const PrioritizeModal = () => {
  return (
    <View>
      <Text>PrioritizeModal</Text>
    </View>
  );
};

export default PrioritizeModal;
